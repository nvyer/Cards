import { Grid, Typography } from "@mui/material";

export const Footer = () => {
  return (
    <Grid item sm={1} xs={1} borderTop="1px solid black">
      <Typography m={2}>footer</Typography>
    </Grid>
  );
};
